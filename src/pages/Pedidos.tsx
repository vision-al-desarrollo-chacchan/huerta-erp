import { useEffect, useMemo, useState } from 'react';
import { getOrders, orderTotal, subscribeRestaurantData } from '../services/restaurant-store';
import type { RestaurantOrder } from '../types/restaurant';
import { useNotification } from '../context/notification-context';
import { userErrorMessage } from '../lib/errors';
const money=new Intl.NumberFormat('es-PE',{style:'currency',currency:'PEN'});
export default function Pedidos(){const [rows,setRows]=useState<RestaurantOrder[]>([]),[filter,setFilter]=useState('todos'),[error,setError]=useState(''),[live,setLive]=useState(false);const {notify}=useNotification();useEffect(()=>{let active=true;let unsubscribe:(()=>void)|undefined;const load=()=>getOrders().then(data=>{if(active){setRows(data);setError('')}}).catch((reason:unknown)=>{const message=userErrorMessage(reason,'No se pudieron cargar los pedidos.');if(active){setError(message);notify(message,'error')}});void load();void subscribeRestaurantData(load,status=>{if(!active)return;setLive(status==='connected');if(status==='error')notify('Se perdió la actualización en tiempo real. Intentando reconectar…','error')}).then(cleanup=>{unsubscribe=cleanup}).catch((reason:unknown)=>{const message=userErrorMessage(reason,'No se pudo iniciar la actualización en tiempo real.');setError(message);notify(message,'error')});return()=>{active=false;unsubscribe?.()}},[notify]);const visible=useMemo(()=>filter==='todos'?rows:rows.filter(r=>r.status===filter),[rows,filter]);return <div className="space-y-6 p-6"><div><h1 className="text-3xl font-black text-slate-950">Pedidos</h1><p className="font-medium text-slate-600">Seguimiento real desde la comanda hasta el cobro.</p><span className={`text-xs font-bold ${live?'text-emerald-600':'text-amber-600'}`}>{live?'● En tiempo real':'● Conectando…'}</span></div>{error&&<div className="rounded-xl bg-red-50 p-4 font-bold text-red-700">{error}</div>}<div className="flex flex-wrap gap-2">{['todos','nuevo','preparando','listo','entregado','pagado','anulado'].map(s=><button key={s} onClick={()=>setFilter(s)} className={`rounded-full px-4 py-2 text-sm font-black capitalize ${filter===s?'bg-blue-600 text-white':'bg-white text-slate-600'}`}>{s}</button>)}</div><section className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left"><thead className="bg-slate-50 text-xs font-black uppercase text-slate-600"><tr><th className="p-4">Pedido</th><th>Servicio</th><th>Detalle</th><th>Estado</th><th>Pago</th><th>Total</th></tr></thead><tbody className="divide-y">{visible.length===0?<tr><td colSpan={6} className="p-12 text-center text-slate-500">No hay pedidos en este estado.</td></tr>:visible.map(r=><tr key={r.id}><td className="p-4 font-black">#{String(r.number).padStart(3,'0')}<small className="block font-medium text-slate-500">{new Date(r.createdAt).toLocaleString('es-PE')}</small></td><td className="capitalize">{r.serviceType}<small className="block text-slate-500">{r.table||r.customer||'—'}</small></td><td>{r.items.reduce((n,i)=>n+i.quantity,0)} productos</td><td><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-black text-blue-700">{r.status}</span></td><td>{r.paymentMethod||'Pendiente'}</td><td className="font-black">{money.format(orderTotal(r))}</td></tr>)}</tbody></table></section></div>}
